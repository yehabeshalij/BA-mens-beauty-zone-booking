'use strict';

/**
 * decorappointment controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::decorappointment.decorappointment');
